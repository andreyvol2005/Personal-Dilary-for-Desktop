<Window x:Class="Personal_Diary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Личный дневник" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#0D1117">

    <Window.Resources>
        <!-- Цветовая схема в стиле страниц -->
        <SolidColorBrush x:Key="PrimaryBackground" Color="#0D1117"/>
        <SolidColorBrush x:Key="SecondaryBackground" Color="#21262D"/>
        <SolidColorBrush x:Key="SurfaceBackground" Color="#30363D"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#B0B3B8"/>
        <SolidColorBrush x:Key="AccentColor" Color="#4CAF50"/>
        <SolidColorBrush x:Key="AccentHover" Color="#66BB6A"/>
        <SolidColorBrush x:Key="SelectionColor" Color="#3366CC"/>
        <SolidColorBrush x:Key="SelectionHover" Color="#4A90E2"/>
        <SolidColorBrush x:Key="DatesActiveColor" Color="#1E88E5"/>
        <SolidColorBrush x:Key="DatesHoverColor" Color="#42A5F5"/>

        <!-- Стиль  кнопки (Dates) -->
        <Style x:Key="ActiveNavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource DatesActiveColor}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="16,0"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectionHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль  кнопки (Notes) -->
        <Style x:Key="InactiveNavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryText}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="12,0"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SecondaryBackground}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource SelectionHover}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--Стиль для кнопок переключения на дневник и заметки-->
        <Style x:Key="MediumModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="6,0"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                        Background="{TemplateBinding Background}"
                        CornerRadius="20"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Навигационная панель -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBackground}" 
                Height="64" BorderBrush="{StaticResource SurfaceBackground}">
            <DockPanel VerticalAlignment="Center">
                <!-- Логотип/Название -->
                <TextBlock Text="Личный дневник" 
                           FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource PrimaryText}"
                           VerticalAlignment="Center"
                           Margin="24,0,32,0"
                           DockPanel.Dock="Left"/>

                <!-- Навигационные кнопки -->
                <DockPanel HorizontalAlignment="Left">
                    <Button x:Name="btnDates" Content="Дневник" Width="100" Height="40"
                    Style="{StaticResource MediumModernButtonStyle}" FontSize="16"
                    Click="BtnDates_Click"/>
                    <Button x:Name="btnNotes" Content="Заметки" Width="100" Height="40"
                    Style="{StaticResource MediumModernButtonStyle}" FontSize="16"
                    Click="BtnNotes_Click"/>
                </DockPanel>
            </DockPanel>
        </Border>

        <!-- Фрейм для отображения страниц -->
        <Frame x:Name="MainFrame" 
               Grid.Row="1"
               NavigationUIVisibility="Hidden"
               Background="{StaticResource PrimaryBackground}"/>
    </Grid>
</Window>

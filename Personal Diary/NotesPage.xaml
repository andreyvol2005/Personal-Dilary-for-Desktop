<Page x:Class="Personal_Diary.Pages.NotesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="NotesPage" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель: Список заметок -->
        <StackPanel Grid.Column="0" Margin="5">
            <!-- Заголовок -->
            <TextBlock Text="Коллекция notes" 
                       FontWeight="Bold" FontSize="14"
                       Margin="0,0,0,10" HorizontalAlignment="Center"/>

            <!-- Кнопки управления заметками -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="btnNew" Content="Новая" Width="80" Height="30" 
                        Margin="0,0,5,0" Click="BtnNew_Click"/>
                <Button x:Name="btnRefresh" Content="⟳" Width="30" Height="30" 
                        Click="BtnRefresh_Click" ToolTip="Обновить список"/>
            </StackPanel>

            <!-- Список заметок -->
            <TextBlock Text="Заметки:" Margin="0,0,0,5"/>
            <ListBox x:Name="lstEntries" Height="300" 
                     SelectionChanged="LstEntries_SelectionChanged">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="Margin" Value="0,0,0,2"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </StackPanel>

        <!-- Правая панель: Редактирование -->
        <StackPanel Grid.Column="1" Margin="5">
            <!-- Название заметки -->
            <TextBlock Text="Название заметки:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtNoteName" Height="30" Margin="0,0,0,10"
                     FontSize="14"/>

            <!-- Текст заметки -->
            <TextBlock Text="Текст заметки:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtMessage" Height="300" 
                     AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" FontSize="14"/>

            <!-- Кнопки действий -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="btnSave" Content="Создать" Width="80" Height="30" 
                        Margin="0,0,5,0" Click="BtnSave_Click"/>
                <Button x:Name="btnDelete" Content="Удалить" Width="80" Height="30" 
                        IsEnabled="False" Click="BtnDelete_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>